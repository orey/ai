# TODO

* Externalize the management of specific files like `pdf`, `pptx` or `xlsx`.
* For `xlsx`, implement a smart analyzer that analyzes if there are several hundreds of void lines. For columns, it can be trickier.
* Define a tag system to delimit chunks by tags.

